# macOS
.DS_Store

# Claude Code project files
.claude/

# Test & development folders (not part of the release)
test/
docs/
Message-Tests/
Docs/
BASURAS/

# NOTE: source.html is listed below but is TRACKED — .gitignore does not apply
# to a file already in the index, and Vercel's build needs it.

# Generated / export files
DDL-Compliance-Report.html
messages-export-*.txt

# Utilities
release-checksum.py
dist/
node_modules/
source.html
index.js.map

# Office binaries (tracked as exploded XML in .cortex/)
*.{xlsx,xlsm,docx,dotx,pptx}
# Sync manifests (local cache, regenerable)
.cortex/manifests/
.vercel
